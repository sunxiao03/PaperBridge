<Window x:Class="PaperBridge.App.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入名称"
        Width="390"
        Height="170"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Margin="0,0,0,8" Foreground="#263D4C" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Height="32" Padding="7,4" MaxLength="80" />
        <StackPanel Grid.Row="2" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="76" Margin="0,0,8,0" IsCancel="True" Content="取消" />
            <Button Width="76" IsDefault="True" Click="ConfirmButton_Click" Content="确定" />
        </StackPanel>
    </Grid>
</Window>
