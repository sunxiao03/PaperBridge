<Window x:Class="PaperBridge.App.AnnotationEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存批注"
        Width="520"
        Height="390"
        MinWidth="440"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="DescriptionText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
        <TextBox x:Name="NoteTextBox" Grid.Row="1" Margin="0,12,0,10" Padding="8" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" ToolTip="可选备注" />
        <DockPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Width="80" VerticalAlignment="Center" Text="颜色" />
            <ComboBox x:Name="ColorCombo" Width="180" HorizontalAlignment="Left" DisplayMemberPath="Label" SelectedValuePath="Value" />
        </DockPanel>
        <CheckBox x:Name="LinkTranslationCheckBox" Grid.Row="3" Content="关联当前译文快照" />
        <StackPanel Grid.Row="4" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="84" Margin="0,0,8,0" IsCancel="True" Content="取消" />
            <Button Width="84" IsDefault="True" Click="SaveButton_Click" Content="保存" />
        </StackPanel>
    </Grid>
</Window>
